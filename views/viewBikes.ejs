<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Bikes</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* Reset and Base Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      padding: 1rem;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 2rem;
      text-align: center;
      animation: fadeIn 1s ease-out;
    }

    .container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1.5rem;
      width: 100%;
      max-width: 1200px;
    }

    .card-wrapper {
      perspective: 1200px;
      width: 300px;
    }

    .card {
      position: relative;
      width: 100%;
      height: 400px;
      background: #1e272e;
      border-radius: 12px;
      overflow: hidden;
      transform-style: preserve-3d;
      transition: transform 0.8s ease-in-out;
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
    }

    /* Remove Flip to Keep Buttons Visible */
    .card-front, .card-back {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      backface-visibility: hidden;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      padding: 1rem;
    }

    .card-front {
      background: #222f3e;
      color: #ffd700;
    }

    .card-front img {
      width: 100%;
      height: 200px;
      object-fit: cover;
      border-radius: 8px;
    }

    .card-front h2 {
      margin-top: 1rem;
      font-size: 1.5rem;
      text-align: center;
    }

    .card-back {
      background: #1e272e;
      color: #ffd700;
      transform: rotateY(180deg);
      justify-content: center;
      gap: 1rem;
    }

    .card button {
      background: #ffd700;
      color: #1e272e;
      border: none;
      padding: 10px 15px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1rem;
      font-weight: bold;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card button:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 12px rgba(255, 215, 0, 0.3);
    }

    .card-back form button {
      background: #ff4b5c;
      color: #fff;
    }

    .card-back form button:hover {
      background: #e84118;
    }

    .back-button {
      margin-top: 2rem;
    }

    .back-button a {
      text-decoration: none;
      color: #ffd700;
      background: #1e272e;
      padding: 10px 30px;
      border-radius: 8px;
      font-size: 1.2rem;
      font-weight: bold;
      border: 2px solid #ffd700;
      transition: background 0.3s ease, transform 0.3s ease;
    }

    .back-button a:hover {
      background: #ffd700;
      color: #1e272e;
      transform: scale(1.1);
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .container {
        flex-direction: column;
        align-items: center;
      }

      .card-wrapper {
        width: 100%;
        max-width: 350px;
      }
    }
  </style>
</head>
<body>

  <h1>View Bikes</h1>

  <div class="container">
    <% bikes.forEach(bike => { %>
      <div class="card-wrapper">
        <div class="card" data-bike-id="<%= bike._id %>">
          <!-- Front Side -->
          <div class="card-front">
            <img src="<%= bike.imageUrl %>" alt="<%= bike.name %>">
            <h2><%= bike.name %></h2>
            <div class="button-group">
              <button onclick="window.location.href='/admin/update-bike/<%= bike._id %>'">Update</button>
              <button onclick="window.location.href='/admin/view-bike/<%= bike._id %>'">View Details</button>
            </div>
          </div>
          <!-- Back Side (Removed for Simplicity) -->
        </div>
      </div>
    <% }) %>
  </div>

  <div class="back-button">
    <a href="/admin/dashboard">Back to Dashboard</a>
  </div>

  <script>
    // Removed the flip effect for now
  </script>

</body>
</html>
