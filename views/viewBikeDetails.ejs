<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Bike Details</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* Reset and Base Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      padding: 1rem;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 2rem;
      text-align: center;
      animation: fadeIn 1s ease-out;
    }

    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 2rem;
      width: 100%;
      max-width: 900px;
    }

    .bike-details {
      background: #1e272e;
      padding: 2rem;
      border-radius: 12px;
      width: 100%;
      max-width: 800px;
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
    }

    .bike-details img {
      width: 100%;
      max-height: 300px;
      object-fit: cover;
      border-radius: 8px;
    }

    .bike-details h2 {
      margin-top: 1rem;
      font-size: 2rem;
      color: #ffd700;
      text-align: center;
    }

    .bike-details p {
      font-size: 1.2rem;
      color: #ddd;
      margin-top: 1rem;
    }

    .bike-details .price {
      font-size: 1.5rem;
      color: #ffd700;
      margin-top: 1rem;
    }

    .bike-details .quantity {
      font-size: 1.2rem;
      color: #ffd700;
      margin-top: 1rem;
    }

    .button-group {
      display: flex;
      justify-content: center;
      gap: 1rem;
      margin-top: 2rem;
    }

    .button-group button,
    .back-button a {
      padding: 10px 20px;
      background: #ffd700;
      color: #1e272e;
      font-weight: bold;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .button-group button:hover,
    .back-button a:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 12px rgba(255, 215, 0, 0.3);
    }

    .back-button {
      margin-top: 2rem;
    }

    .back-button a {
      text-decoration: none;
      color: #ffd700;
      background: #1e272e;
      padding: 10px 30px;
      border-radius: 8px;
      font-size: 1.2rem;
      font-weight: bold;
      border: 2px solid #ffd700;
      transition: background 0.3s ease, transform 0.3s ease;
    }

    .back-button a:hover {
      background: #ffd700;
      color: #1e272e;
      transform: scale(1.1);
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

  </style>
</head>
<body>

  <h1>View Bike Details</h1>

  <div class="container">
    <div class="bike-details">
      <img src="<%= bike.imageUrl %>" alt="<%= bike.name %>">
      <h2><%= bike.name %></h2>
      <p><strong>Type:</strong> <%= bike.type %></p>
      <p><strong>Selling Price:</strong> $<%= bike.sellingPrice %></p>
      <p><strong>Rental Price per Day:</strong> $<%= bike.rentalPricePerDay %></p>
      <p class="price"><strong>Total Quantity:</strong> <%= bike.quantity %></p>
    </div>

    <div class="button-group">
      <button onclick="window.location.href='/admin/update-bike/<%= bike._id %>'">Update Bike</button>
      <form action="/admin/delete-bike/<%= bike._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this bike?');">
        <button type="submit">Delete Bike</button>
      </form>
    </div>

    <div class="back-button">
      <a href="/admin/view-bikes">Back to Bikes List</a>
    </div>
  </div>

  <script>
    // Any additional JavaScript, if needed
  </script>

</body>
</html>
