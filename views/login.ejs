<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Royal Bike Club</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <style>
    /* Custom Styles for Login Page */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: url('https://images.unsplash.com/photo-1558980664-1db506751c6c?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D') no-repeat center center/cover;
      height: 100vh;
      color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      position: relative;
    }

    /* Dark overlay */
    body::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.6); /* Dark overlay */
      z-index: -1;
    }

    nav ul {
      list-style: none;
      display: flex;
      justify-content: center;
      /* background-color: #4a148c; */
      margin: 0;
      padding: 0.5rem 0;
    }

    nav ul li {
      margin: 0 15px;
    }

    nav ul li a {
      text-decoration: none;
      color: #ffd700;
      font-weight: bold;
      font-size: 1rem;
    }

    nav ul li a:hover {
      color: #ffcc00;
    }

    .container {
      max-width: 400px;
      margin: auto;
      padding: 2rem;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    }

    .container h2 {
      text-align: center;
      margin-bottom: 2rem;
      font-size: 2rem;
      color: #ffd700;
      font-weight: bold;
    }

    form input {
      width: 100%;
      padding: 0.8rem;
      margin-bottom: 1.5rem;
      border: none;
      border-radius: 5px;
      font-size: 1rem;
    }

    form input:focus {
      outline: none;
      box-shadow: 0 0 5px #ffd700;
    }

    form button {
      width: 100%;
      padding: 0.8rem;
      background: #ffd700;
      border: none;
      border-radius: 5px;
      font-size: 1.2rem;
      color: #4a148c;
      font-weight: bold;
      cursor: pointer;
    }

    form button:hover {
      background: #ffcc00;
    }

    .forgot-password {
      text-align: center;
      margin-top: 1rem;
    }

    .forgot-password a {
      color: #ffd700;
      text-decoration: none;
      font-size: 0.9rem;
    }

    .forgot-password a:hover {
      text-decoration: underline;
    }

    footer {
      text-align: center;
      /* background: #4a148c; */
      color: #ffd700;
      padding: 1rem 0;
    }

    footer a {
      color: #ffd700;
      text-decoration: none;
    }

    footer a:hover {
      text-decoration: underline;
    }

    /* Error message styles */
    .alert {
      margin-bottom: 1.5rem;
      background-color: #f44336;
      color: white;
      padding: 10px;
      border-radius: 5px;
      text-align: center;
    }
  </style>
</head>
<body>
  <!-- Navigation -->
  <nav>
    <ul>
      <li><a href="/">Home</a></li>
      <li><a href="/register">Register</a></li>
      <li><a href="/login">Login</a></li>
    </ul>
  </nav>

  <!-- Login Form -->
  <div class="container">
    <h2>Login to Your Account</h2>

    <!-- Error Message Display: Only show if message exists -->
    <% if (typeof message !== 'undefined' && message) { %>
      <div class="alert">
        <%= message %>
      </div>
    <% } %>

    <form action="/login" method="POST">
      <input type="email" name="email" placeholder="Email" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit">Login</button>
    </form>
    <div class="forgot-password">
      <a href="/forgot-password">Forgot your password?</a>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    <p>&copy; 2024 Royal Bike Club. All rights reserved.</p>
    <p><a href="#">Contact Us</a> | <a href="#">Privacy Policy</a></p>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
