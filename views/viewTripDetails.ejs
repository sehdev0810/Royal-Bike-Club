<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Trip Details</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* Reset and Base Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #ff7e5f, #feb47b);
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      min-height: 100vh;
      text-align: center;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: #fff;
    }

    .trip-image {
      width: 80%;
      max-width: 600px;
      height: auto;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4);
      margin-bottom: 2rem;
      object-fit: cover;
    }

    .trip-info {
      background-color: #2c3e50;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
      width: 100%;
      max-width: 800px;
      margin-bottom: 2rem;
      text-align: left;
    }

    .trip-info h2 {
      font-size: 1.6rem;
      margin-bottom: 1rem;
      color: #ffd700;
    }

    .trip-info p {
      font-size: 1.1rem;
      margin-bottom: 1rem;
    }

    .price {
      font-size: 1.4rem;
      font-weight: bold;
      color: #ffd700;
      margin-bottom: 2rem;
    }

    .back-button {
      text-decoration: none;
      color: #fff;
      background: #1e272e;
      padding: 12px 30px;
      border-radius: 8px;
      font-size: 1.2rem;
      font-weight: bold;
      border: 2px solid #ffd700;
      transition: background 0.3s ease, transform 0.3s ease;
    }

    .back-button:hover {
      background: #ffd700;
      color: #1e272e;
      transform: scale(1.1);
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      h1 {
        font-size: 2rem;
      }

      .trip-image {
        width: 100%;
        max-width: 400px;
      }

      .trip-info {
        padding: 1.5rem;
      }

      .price {
        font-size: 1.2rem;
      }

      .back-button {
        font-size: 1rem;
        padding: 8px 20px;
      }
    }
  </style>
</head>
<body>

  <h1><%= trip.title %></h1>

  <!-- Trip Image -->
  <img src="<%= trip.imageUrl %>" alt="<%= trip.title %>" class="trip-image">

  <!-- Trip Details -->
  <div class="trip-info">
    <!-- Description -->
    <h2>Description:</h2>
    <p><%= trip.description %></p>

    <!-- Start Date -->
    <h2>Start Date:</h2>
    <p><%= trip.startDate ? trip.startDate.toDateString() : 'N/A' %></p>

    <!-- End Date -->
    <h2>End Date:</h2>
    <p><%= trip.endDate ? trip.endDate.toDateString() : 'N/A' %></p>

    <!-- Total Seats -->
    <h2>Total Seats:</h2>
    <p><%= trip.totalSeats %></p>

    <!-- Number of Seats Left -->
    <h2>Seats Left:</h2>
    <p><%= trip.numberOfSeatsLeft %></p>

    <!-- Price -->
    <p class="price">Price: $<%= trip.price %></p>
  </div>

  <!-- Back Button -->
  <a href="/admin/view-trips" class="back-button">Back to All Trips</a>

</body>
</html>
